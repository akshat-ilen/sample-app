import { SmartDateDirective } from './smart-date.directive';

describe('SmartDateDirective', () => {
  it('should create an instance', () => {
    const directive = new SmartDateDirective();
    expect(directive).toBeTruthy();
  });
});
