import { DeltaDirDirective } from './delta-dir.directive';

describe('DeltaDirDirective', () => {
  it('should create an instance', () => {
    const directive = new DeltaDirDirective();
    expect(directive).toBeTruthy();
  });
});
